name: Test preview_before_deploy enabled
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create dist directory with sample files
        run: |
          mkdir -p "$GITHUB_WORKSPACE/public"
          echo "<html><body>Preview Test</body></html>" > "$GITHUB_WORKSPACE/public/index.html"
          echo "body { font-size: 16px; }" > "$GITHUB_WORKSPACE/public/style.css"

      - id: action
        uses: .
        continue-on-error: true
        with:
          api_key: fake-api-key-for-testing
          dist_dir: public
          cleanup: 'false'
          neocities_supporter: 'false'
          preview_before_deploy: 'true'

      - name: Assert action failed at API level (expected with fake key)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail with fake API key and preview_before_deploy=true, but got outcome: $OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed with fake API key (preview_before_deploy=true validated)"
